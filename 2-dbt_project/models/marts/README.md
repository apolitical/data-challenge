# Marts layer

Create your final mart model(s) here. The brief asks for one row per course, materialized as a table (see `dbt_project.yml`).

`_placeholder.sql` is disabled (`{{ config(enabled=false) }}`) and exists only so dbt does not complain about an empty folder. Delete it once you add a real model.
