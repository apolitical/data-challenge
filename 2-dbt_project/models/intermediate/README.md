# Intermediate layer

Create your intermediate models here. This layer typically joins base models and computes per-entity aggregates that the marts layer can read from.

`_placeholder.sql` is disabled (`{{ config(enabled=false) }}`) and exists only so dbt does not complain about an empty folder. Delete it once you add a real model.
