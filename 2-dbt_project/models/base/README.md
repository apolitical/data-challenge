# Base layer

Create your base models here, one per source table, lightly cleaned (rename, cast, dedupe).

`legacy.sql` in this folder is a starter reference showing how the messy query can be brought into dbt with `{{ source(...) }}` refs. It is safe to edit, replace, or delete.
